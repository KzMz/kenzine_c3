module kenzine::faults;

fault SystemError
{
    STATE_IS_NULL,
    PARAMS_IS_NULL,
}

fault ResourceSystemError
{
    BASE_ASSET_PATH_EMPTY,
    RESOURCE_LOADER_ALREADY_REGISTERED,
    RESOURCE_LOADER_INVALID_TYPE,
    RESOURCE_LOADERS_LIMIT_REACHED,
    RESOURCE_LOADER_NOT_FOUND,
    RESOURCE_LOADER_USE_LOAD_CUSTOM_INSTEAD,
    LOAD_FAILED,
}